<h1><%= @page.title %></h1>
<p><em><%= time_ago_in_words @page.created_at %></em></p>



<p> <em>  <%= time_ago_in_words(@page.created_at) %></em></p>
<dl>
  <%= @page.fields.each do |field| %>
    <dt><%= field.field_definition.key %></dt>
    <dd><%= field.value %></dd>
  <% end %>
</dl>

<p>
  <strong>
    <%= simple_format @page.body %>
  </strong>
</p>
