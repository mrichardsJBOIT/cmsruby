=form_with url: url, model: @type do |form|
  %p
    =form.text_field :name, placeholder: "Name", class: "form-control input-lg"

  %h3 Custom fields

  =form.fields_for :field_definitions do |c|
    %p
      =c.text_field :key, class: "form-control"
      =c.hidden_field :id

  %p
    =form.submit
